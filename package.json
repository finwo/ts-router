{
  "version": "0.2.4",
  "workspaces": [
    "packages/router-fastify"
  ],
  "scripts": {
    "install:all": "npm install --workspaces",
    "build:all": "npm run build --workspaces --if-present",
    "version": "VERSION=$(jq -r '.version' package.json) ; for d in $(jq -r '.workspaces[]' package.json); do (cd $d; jq \".version = \\\"$VERSION\\\"\" package.json | sponge package.json); done",
    "public:all": "npm run publish --workspaces --if-present"
  }
}
